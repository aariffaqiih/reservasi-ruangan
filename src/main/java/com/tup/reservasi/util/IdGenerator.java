package com.tup.reservasi.util;

/*
 * Penanggung jawab: seluruh anggota kelompok.
 *
 * Arahan dari class-diagram:
 * - Banyak entity memakai id bertipe String:
 *   User.id, Room.roomId, Reservation.reservationId, Approval.approvalId,
 *   AccessRecord.recordId, Notification.notificationId.
 * - Jika tim memakai generator manual, sepakati pola prefix id untuk tiap entity.
 * - Jika tim memakai generated value database, file ini boleh dibiarkan tidak dipakai.
 * - Jangan isi implementasi sebelum strategi id disepakati.
 */
